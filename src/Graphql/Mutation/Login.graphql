mutation Login($prmLoginInput: LoginInputTypeInput!) {
  login(loginInput: $prmLoginInput) {
    resultType
    messageText
    data {
      subscriptionId
      perNo
      userDesc
      userroles {
        email
        roleCode
        subscriptionid
      }
    }
  }
}
